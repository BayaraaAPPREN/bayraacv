/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
}

module.exports = {
  env: {
    PUBLIC_URL: "https://bayaraaappren.github.io/bayraacv/",
    assetPrefix: './'
  }
};
